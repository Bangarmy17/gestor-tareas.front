import { TareaDetail } from "./TareaDetail"

export const TareaGrid = ({handlerRemove, tareas = [] }) => {
    return(
        <div>
             {tareas.map(tarea => {
            return <TareaDetail handlerRemove={handlerRemove} tarea={tarea} key={tarea.id}/>
        })}
        </div>
    )
}