export const TareaDetail = ({ handlerRemove,tarea ={}}) => {
    return(
        <div>
            <li>{tarea.nombre} {tarea.descripcion} {tarea.completa}</li>
            <button onClick={()=>handlerRemove(tarea.id)}>Eliminar</button>
        </div>
        
    )
}
